version: '2'
services:
  policy-manager:
    privileged: true
    network_mode: host
    pid: host
    image: leodotcloud/policy-manager:dev
    labels:
      io.rancher.scheduler.global: 'true'
    logging:
      driver: json-file
      options:
        max-size: 25m
        max-file: '2'
